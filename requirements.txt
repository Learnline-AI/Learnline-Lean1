# Live Transcription - Minimal Railway Dependencies
# Let RealtimeSTT handle its own audio processing dependencies

# Core Web Framework (Essential)
fastapi==0.104.1
uvicorn[standard]==0.24.0
websockets==12.0
python-dotenv==1.0.0

# Core STT (Handles its own scipy, numpy, torch dependencies)
RealtimeSTT==0.3.104